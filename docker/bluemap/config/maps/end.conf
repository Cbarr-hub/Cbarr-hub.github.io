# The End. Same world folder as the overworld (mounted at /app/world via the
# compose volume subpath); BlueMap resolves the end region from the dimension id.
# Renders only once the End has been generated/visited.
world: "/app/world"
dimension: "minecraft:the_end"
name: "End"
sorting: 20

ambient-light: 0.5
sky-color: "#100d1a"
void-color: "#0a0010"
