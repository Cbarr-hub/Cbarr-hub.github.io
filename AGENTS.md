# Gamertown — Agent Context

**Single source of truth: [`CLAUDE.md`](CLAUDE.md).**

This repo keeps all project context (stack, infra facts, common operations, and the
dense list of known gotchas) in `CLAUDE.md`. To avoid two copies drifting out of
sync, this file is intentionally a pointer rather than a duplicate.

Any agent (Codex, Claude, or otherwise) working in this repo should read
`CLAUDE.md` first. Prose docs live in [`docs/`](docs/) (index:
[`docs/README.md`](docs/README.md)).
