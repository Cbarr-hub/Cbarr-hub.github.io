# Local Claude Code state — settings + worktree checkouts. Never commit:
# .claude/worktrees/ holds full working copies of the repo.
.claude/

# macOS junk
.DS_Store

# Logs
*.log

# Local secrets — NEVER commit (this is a public *.github.io repo)
SECRETS.local.md
*.local
.secrets
.env             # runtime env (root project .env has GSLTs; backend/.env) — NOT .env.example
secrets.env      # host secrets file, if ever copied into the repo dir
