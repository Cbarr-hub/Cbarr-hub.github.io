# BlueMap built-in webserver. Caddy reverse-proxies /map/* to bluemap:8100 on the
# compose network (the port is NOT host-published), gated behind the auth gate.
enabled: true
ip: "0.0.0.0"
port: 8100
webroot: "/app/web"
